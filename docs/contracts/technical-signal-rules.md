# Technical signal rule catalog

This catalog covers the 52 `EvidenceInput` emission sites registered in `SIGNAL_RULE_VERSIONS` at version `1.0.0`. Source locations refer to files under `services/backend/src/cci/analyzers/`. The detector conditions and strengths below describe current code, including branches and caps; they are not a proficiency scale. Numeric strengths are **uncalibrated policy heuristics**. The repository records no empirical basis for any individual numeric weight. A rule can emit several observations when several matching lines or constructs are present. Emission does not establish who wrote the artifact, whether code ran, whether a deployment works beyond the inspected response, or whether a person is proficient. Attribution and coverage are evaluated separately.

| Rule ID | Version | Analyzer / source | Detection condition | Signal strength | Rationale | Limitations |
|---|---|---|---|---|---|---|
| `candidatex.code.python.dependency_import` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor._append_dependency_import` | An absolute Python `import` or `from` names a top-level package found in `DEPENDENCY_CAPABILITY_MAP` after normalization | `55.0` | A recognized import suggests use of a relevant library | Import syntax alone does not show meaningful use or execution |
| `candidatex.code.python.async_function` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor.visit_AsyncFunctionDef` | AST contains an async function definition | `75.0` | Async syntax is a backend technical signal | Syntax does not establish concurrency behavior or execution |
| `candidatex.code.python.api_route` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor._analyze_function_common` | Function decorator is a call whose attribute is `get`, `post`, `put`, `delete`, `patch`, or `route`, or a bare attribute with one of the first five names | `80.0` | A route decorator suggests an API handler | Decorator names are not tied to a verified framework or running endpoint |
| `candidatex.code.python.dependency_injection` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor._analyze_function_common` | At least one function default's unparsed AST contains `Depends(` | `82.0` | Dependency injection syntax suggests explicit wiring | String match does not verify dependency behavior |
| `candidatex.code.python.auth_guard` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor._analyze_function_common` | Unparsed decorator contains `auth`, `login_required`, `roles`, or `permissions`, or an unparsed function default contains `auth`, `current_user`, `security`, or `token`, case insensitive | `85.0` | Names suggest an authentication or authorization guard | Names do not verify that access control is effective |
| `candidatex.code.python.pydantic_schema` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor.visit_ClassDef` | A class base name contains `basemodel` or `schema`, case insensitive | `78.0` | Model-like inheritance suggests schema structure | Base-name match does not prove validation quality |
| `candidatex.code.python.ml_class` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor.visit_ClassDef` | A class base name contains `module`, `dataset`, or `baseestimator`, case insensitive | `85.0` | Inheritance suggests an ML model or dataset | Broad base-name match does not verify training or model quality |
| `candidatex.code.python.architecture_class` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor.visit_ClassDef` | Class name ends exactly with `Service`, `Repository`, `Controller`, or `Handler` | `80.0` | Naming suggests a layered component | Naming does not establish separation of concerns |
| `candidatex.code.python.try_except` | `1.0.0` | `code/python_analyzer.py:PythonStructuralVisitor.visit_Try` | AST contains a `try` statement | `72.0` | A try block suggests error handling | Presence does not establish correct recovery or exception scope |
| `candidatex.code.typescript.api_route` | `1.0.0` | `code/multi_language.py:analyze_typescript_javascript` | One line matches `app` or `router` followed by `.get`, `.post`, `.put`, `.delete`, or `.patch`, an opening call, and a quoted path | `78.0` | Route registration text suggests a web handler | Line regex does not parse JavaScript or verify a reachable route |
| `candidatex.code.typescript.async_handler` | `1.0.0` | `code/multi_language.py:analyze_typescript_javascript` | One line matches `async` followed by `function`, a parenthesized argument list, or an identifier and `(` | `74.0` | Async syntax suggests asynchronous handling | Text match does not establish execution or correct async behavior |
| `candidatex.code.typescript.zod_schema` | `1.0.0` | `code/multi_language.py:analyze_typescript_javascript` | One line contains a word-boundary `z.object(` with optional whitespace | `80.0` | Schema construction suggests validation intent | Text match does not verify schema use or coverage |
| `candidatex.code.typescript.try_catch` | `1.0.0` | `code/multi_language.py:analyze_typescript_javascript` | One line contains `try` followed by optional whitespace and `{` | `70.0` | A try block suggests error handling | The regex does not inspect a catch branch or runtime behavior |
| `candidatex.code.go.http_route` | `1.0.0` | `code/multi_language.py:analyze_go_source` | One line matches `HandleFunc`, `.GET`, `.POST`, `.PUT`, or `.DELETE` call with a quoted path | `82.0` | Handler registration suggests an HTTP route | Text match does not verify a serving application |
| `candidatex.code.go.goroutine_channel` | `1.0.0` | `code/multi_language.py:analyze_go_source` | One line contains `go func(` or `make(chan` with optional whitespace | `85.0` | Goroutine or channel syntax suggests concurrency | Syntax does not establish safety or useful concurrency |
| `candidatex.code.go.error_guard` | `1.0.0` | `code/multi_language.py:analyze_go_source` | Trimmed line contains exact text `if err != nil` | `72.0` | An error guard suggests explicit handling | Presence does not establish propagation or recovery correctness |
| `candidatex.code.java.spring_endpoint` | `1.0.0` | `code/multi_language.py:analyze_java_source` | One line contains `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`, or `@RequestMapping` | `80.0` | Spring annotation suggests an endpoint | Text match does not verify mapping or running service |
| `candidatex.code.java.spring_component` | `1.0.0` | `code/multi_language.py:analyze_java_source` | One line contains `@Service`, `@Repository`, or `@Component` | `80.0` | Annotation suggests component organization | Text match does not verify architecture or use |
| `candidatex.code.cpp.smart_pointer_raii` | `1.0.0` | `code/multi_language.py:analyze_c_cpp_source` | One line contains `std::unique_ptr`, `std::shared_ptr`, `std::make_unique`, or `std::make_shared` | `85.0` | Smart pointer syntax suggests managed lifetime | Text presence does not prove correct ownership or RAII behavior |
| `candidatex.code.cpp.stl_algorithm_structure` | `1.0.0` | `code/multi_language.py:analyze_c_cpp_source` | One line contains `std::vector`, `std::unordered_map`, `std::sort`, or `std::priority_queue` | `78.0` | STL use suggests algorithm or data-structure work | Text match does not establish algorithm correctness or complexity |
| `candidatex.dependencies.manifest_declaration` | `1.0.0` | `code/dependencies.py:dependencies_to_evidence` | Parsed `package.json`, requirements text, `pyproject.toml`, `go.mod`, or `pom.xml` dependency name contains a key in `DEPENDENCY_CAPABILITY_MAP` | `55.0` | A declaration suggests a technology dependency | A package listing does not establish use, authorship, or proficiency |
| `candidatex.database.sql_table` | `1.0.0` | `database/schema_analyzer.py:analyze_sql_content` | One line matches case-insensitive `CREATE TABLE`, optional `IF NOT EXISTS`, and a table identifier | `72.0` | Table definition suggests schema work | Static SQL may be unused, invalid, or unapplied |
| `candidatex.database.sql_index` | `1.0.0` | `database/schema_analyzer.py:analyze_sql_content` | One line matches case-insensitive `CREATE`, optional `UNIQUE`, `INDEX`, optional `IF NOT EXISTS`, an index name, `ON` table, and parenthesized columns | `86.0` for more than one comma-separated column; otherwise `82.0` if unique; otherwise `78.0` | Index declaration suggests query-planning intent | Column splitting is textual; index utility and execution are unverified |
| `candidatex.database.sql_foreign_key` | `1.0.0` | `database/schema_analyzer.py:analyze_sql_content` | One line matches case-insensitive `FOREIGN KEY` or `REFERENCES` plus an identifier and `(` | `80.0` | Reference syntax suggests relational constraints | Static declaration does not verify enforced integrity |
| `candidatex.database.sql_advanced_feature` | `1.0.0` | `database/schema_analyzer.py:analyze_sql_content` | One line matches case-insensitive `PARTITION BY`, `CREATE TRIGGER`, or `CREATE` with optional `OR REPLACE` then `FUNCTION` | `88.0` | Syntax suggests advanced database features | Text presence does not establish correct or deployed behavior |
| `candidatex.database.alembic_migration` | `1.0.0` | `database/schema_analyzer.py:analyze_alembic_migration` | Content has exact `def upgrade()` and at least one `op.create_table`, `add_column`, `create_index`, `create_foreign_key`, `alter_column`, or `drop_table`; downgrade is considered reversible only when text after `def downgrade()` through next `def ` contains `op.` and lacks `pass` | `85.0` if considered reversible; otherwise `76.0` | Migration operations suggest schema evolution | Text check does not prove reversibility or safe migration execution |
| `candidatex.database.prisma_schema` | `1.0.0` | `database/schema_analyzer.py:analyze_prisma_schema` | Content matches at least one `model Name {`; optional markers are `@relation`, `@@index`, or `@@unique` | Base `72.0`; add `6.0` for `@relation` and `8.0` for `@@index` or `@@unique`; `min(score, 90.0)`. Possible values: `72.0`, `78.0`, `80.0`, `86.0` | Models and relation/index markers suggest ORM schema design | Text markers do not verify migration or query behavior |
| `candidatex.deployment.live_service` | `1.0.0` | `deployment/inspector.py:inspect_live_deployment` | Safe HTTP fetch or supplied mock response returns status `200` through `299`; CDN hint if a response-header name contains `cf-`, `vercel`, `netlify`, `fastly`, `x-amz-`, or `fly-` | `82.0` with any CDN hint; otherwise `76.0` | A successful response indicates an accessible service | One response does not establish reliability, attribution, or application behavior |
| `candidatex.deployment.security_headers` | `1.0.0` | `deployment/inspector.py:inspect_live_deployment` via `analyze_security_headers` | Successful response has at least two of HSTS, CSP, `X-Content-Type-Options: nosniff`, `X-Frame-Options: deny/sameorigin`, Referrer-Policy, and Permissions-Policy | Base `65.0`; add `8.0` for HSTS, `10.0` for CSP, `5.0` for nosniff, `4.0` for the frame option, and `3.0` if Referrer-Policy or Permissions-Policy is present; `min(score, 95.0)` | Headers suggest defensive configuration | Header presence does not prove effective security policy |
| `candidatex.deployment.responsive_dom` | `1.0.0` | `deployment/inspector.py:inspect_live_deployment` via `analyze_html_structure` | Successful response declares `text/html` and HTML contains a viewport meta tag, one of `<main`, `<nav`, `<header`, `<footer`, `<article`, or an OpenGraph property meta tag | Base `70.0`; add `8.0` for viewport, `7.0` for any semantic tag, and `5.0` for OpenGraph; `min(score, 90.0)` | HTML markers suggest responsive or semantic structure | Static HTML inspection does not render pages or establish usability; title detection does not affect strength |
| `candidatex.deployment.tls_certificate` | `1.0.0` | `deployment/inspector.py:inspect_live_deployment` via `inspect_tls_certificate` | Successful HTTPS response, TLS socket inspection enabled, and certificate inspection returns `is_valid` | `84.0` | TLS handshake suggests a valid certificate at inspection time | Certificate validity does not prove site ownership or ongoing availability |
| `candidatex.docs.readme_setup` | `1.0.0` | `documentation/architecture.py:analyze_readme` | Case-insensitive Markdown heading levels 1–3 begins `getting started`, `installation`, `setup`, `quickstart`, or `prerequisites` | `78.0` | Setup heading suggests onboarding documentation | Heading presence does not verify instructions |
| `candidatex.docs.readme_architecture_diagram` | `1.0.0` | `documentation/architecture.py:analyze_readme` | Content contains a Mermaid fence or PlantUML start marker, case insensitive, or an image Markdown alt text containing `architecture`, `diagram`, `design`, or `flow` | `85.0` | A diagram marker suggests architecture communication | Diagram may be stale, empty, or misleading |
| `candidatex.docs.adr_structure` | `1.0.0` | `documentation/architecture.py:analyze_adr` | Case-insensitive Markdown headings of levels 1–3 for both `context` and `decision` appear | `90.0` | ADR headings suggest recorded design decisions | Consequences are not required; headings do not prove decision quality |
| `candidatex.docs.openapi_spec` | `1.0.0` | `documentation/architecture.py:analyze_openapi_spec` | Parsed YAML or JSON is a mapping with an `openapi` or `swagger` key | `85.0` | Formal API spec suggests endpoint documentation | Paths may be empty and spec validity or implementation is unverified |
| `candidatex.docs.repository_layer_boundaries` | `1.0.0` | `documentation/architecture.py:analyze_layer_boundaries` | Across repository paths, at least three distinct case-normalized directory parts equal `domain`, `services`, `adapters`, `repositories`, `controllers`, `models`, or `api` | `82.0` | Directory names suggest layer separation | Names do not establish actual module boundaries |
| `candidatex.infra.dockerfile` | `1.0.0` | `infra/devops_analyzer.py:analyze_dockerfile` | Every inspected Dockerfile emits; optional features are multiple `FROM` stages or named stage or `COPY --from=`, a non-root `USER`, and `HEALTHCHECK` | Base `72.0`; add `12.0` for multistage or `COPY --from=`, `6.0` for non-root `USER`, and `4.0` for `HEALTHCHECK`; `min(score, 92.0)` | Container directives suggest deployment preparation | File is not built or run; regex can miss or misread instructions |
| `candidatex.infra.docker_compose` | `1.0.0` | `infra/devops_analyzer.py:analyze_docker_compose` | Parsed YAML is a mapping with a `services` key; optional markers are network, volume, and service healthcheck keys | Base `74.0`; add `5.0` for networks, `4.0` for volumes, and `5.0` for healthchecks; `min(score, 90.0)` | Compose keys suggest service orchestration | `services` may be empty; configuration is not executed |
| `candidatex.infra.ci_workflow` | `1.0.0` | `infra/devops_analyzer.py:analyze_ci_workflow` | Parsed YAML is a mapping with `jobs` or `stages`; optional text checks detect a test job or command, both `strategy:` and `matrix:`, `actions/cache` or `cache:`, and deploy job or text | Base `75.0`; add `6.0` for tests, `7.0` for matrix, `4.0` for cache, and `5.0` for deploy; `min(score, 94.0)` | Workflow declarations suggest automated delivery | Text and key presence do not establish successful CI execution |
| `candidatex.infra.kubernetes_manifest` | `1.0.0` | `infra/devops_analyzer.py:analyze_kubernetes_manifest` | Parsed YAML document set has at least one mapping with `kind`; optional textual document markers are `limits` with `cpu` or `memory`, and `livenessprobe` or `readinessprobe` | Base `80.0`; add `7.0` for limits and `5.0` for a probe; `min(score, 92.0)` | Resource manifests suggest orchestration intent | Manifests are not applied; text markers can overstate configuration |
| `candidatex.infra.terraform_hcl` | `1.0.0` | `infra/devops_analyzer.py:analyze_terraform_hcl` | Content matches at least one quoted `resource` type/name or quoted `module` name | Base `82.0`; add `4.0` for resources and `5.0` for modules; `min(score, 92.0)` | Resource or module declarations suggest infrastructure as code | HCL is not parsed, planned, or applied |
| `candidatex.testing.python_suite` | `1.0.0` | `testing/test_analyzer.py:analyze_python_test_file` | Parsed AST has at least one sync or async function named with `test_` prefix or `_test` suffix | `72.0` | Test naming suggests a Python test suite | Tests are never run; names do not establish useful assertions |
| `candidatex.testing.python_fixtures` | `1.0.0` | `testing/test_analyzer.py:analyze_python_test_file` | Parsed AST has at least one function decorator whose unparsed text contains `fixture` | `80.0` | Fixture decorator suggests reusable test setup | Text match does not verify fixture behavior |
| `candidatex.testing.python_parameterized` | `1.0.0` | `testing/test_analyzer.py:analyze_python_test_file` | Parsed AST has at least one function decorator whose unparsed text contains `parametrize` | `84.0` | Parameterization suggests varied test inputs | Decorator presence does not establish meaningful cases |
| `candidatex.testing.python_mocks` | `1.0.0` | `testing/test_analyzer.py:analyze_python_test_file` | Parsed imports contain `mock`, `unittest.mock`, `pytest_mock`, `responses`, or `respx`, or content contains `unittest.mock` or `mocker.` | `85.0` | Mock markers suggest dependency isolation | Mocks can obscure real behavior; tests are not executed |
| `candidatex.testing.python_property_based` | `1.0.0` | `testing/test_analyzer.py:analyze_python_test_file` | Parsed imports contain `hypothesis` or a function decorator contains `given(` | `92.0` | Hypothesis marker suggests generated test cases | Presence does not establish property quality or execution |
| `candidatex.testing.python_regex_fallback` | `1.0.0` | `testing/test_analyzer.py:_regex_fallback_python_test` | Python AST parse raises `SyntaxError` and content regex finds at least one `def test_name(` | `70.0` | Text match recovers test presence from invalid or partial source | Source failed parsing and tests cannot be verified |
| `candidatex.testing.javascript_suite` | `1.0.0` | `testing/test_analyzer.py:analyze_js_ts_test_file` | Content regex finds `it(` or `test(` followed by a quote or backtick | `72.0` | Test-call text suggests JS/TS specifications | Regex does not parse or execute tests |
| `candidatex.testing.javascript_lifecycle` | `1.0.0` | `testing/test_analyzer.py:analyze_js_ts_test_file` | Content regex finds `beforeEach`, `afterEach`, `beforeAll`, or `afterAll` | `78.0` | Hooks suggest test setup and cleanup | Hook presence does not verify isolation |
| `candidatex.testing.javascript_mocks` | `1.0.0` | `testing/test_analyzer.py:analyze_js_ts_test_file` | Content regex finds `jest.mock`, `vi.mock`, `jest.fn`, `vi.fn`, or `sinon.stub` | `84.0` | Mock calls suggest dependency isolation | Regex does not establish correct mock use |
| `candidatex.testing.javascript_supertest` | `1.0.0` | `testing/test_analyzer.py:analyze_js_ts_test_file` | Content regex finds `request(app)` or `supertest` | `86.0` | HTTP-test marker suggests integration testing | Source inspection does not verify HTTP assertions or execution |
| `candidatex.testing.go_suite` | `1.0.0` | `testing/test_analyzer.py:analyze_go_test_file` | Content has a `func TestName(variable *testing.T)` regex match; optional markers are `t.Run(` and `tests := []struct {` or `testCases := []struct {` | `84.0` if table marker; otherwise `80.0` if subtests; otherwise `74.0` | Go test functions suggest test coverage | Text markers do not prove tests run or cover useful behavior |
